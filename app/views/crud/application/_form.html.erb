<%= crud_form resource do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <% columns.each do |column| %>
      <%= simple_form_input f, column %>
    <% end %>
  </div>
  <%= yield :form_actions %>
<% end %>
