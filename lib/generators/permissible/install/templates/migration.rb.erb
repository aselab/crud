class CreatePermissions < ActiveRecord::Migration
  def change
    create_table :permissions do |t|
      t.references :permissible, :polymorphic => true
      t.references :<%= principal_name.underscore %>
      t.integer :flags
    end
    add_index :permissions, [:permissible_id, :permissible_type]
    add_index :permissions, :<%= principal_name.underscore %>_id
  end
end
