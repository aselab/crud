class Permission < ActiveRecord::Base
  belongs_to :principal, :class_name => '<%= principal_name.camelize %>',
    :foreign_key => "principal_id"
  belongs_to :permissible, :polymorphic => true

  attr_accessible :flags, :as => :admin
end
